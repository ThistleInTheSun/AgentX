# 复制为 config.yaml 后按需修改。每日 9:15 推送过去一周内容。
params:
  limit_news: 15
  limit_papers: 10
  # 可选：自定义科技新闻 RSS（默认已为国内可访问源）
  # feed_urls:
  #   - ["https://sspai.com/feed", "少数派"]
  #   - ["https://www.solidot.org/index.rss", "Solidot"]
  #   - ["https://36kr.com/feed", "36氪"]
  # 可选：arXiv 子类
  # arxiv_categories: ["cs.AI", "cs.LG", "cs.CL", "cs.CV"]

schedule:
  time: "09:15"
  window: "7d"
  # timezone: Asia/Shanghai  # 可选，不写则用主 config

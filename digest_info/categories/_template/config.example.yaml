# 复制为 config.yaml 后填写本类别的参数与推送时间
params:
  limit: 10
  # 本类别需要的其他参数，如 feed_url、feed 等

schedule:
  time: "09:15"
  # weekday: 4             # 可选：仅周五（0=周一 … 6=周日）
  # days: [0, 1, 2, 3, 4]  # 可选：周一至周五
  # window: "24h"          # 可选：内容时间窗口
  # timezone: Asia/Shanghai # 可选：不写则用主 config 的 timezone
